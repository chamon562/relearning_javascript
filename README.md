# relearning_javascript

## JSON
- json stands for "javascript object notation"
- is used to represent data
- Generally used for api's to carry information thats very light weight (api's & configuration).